
class Tweet_extractor:
    
    def __init__(self):
        consumer_key=input("Enter OAUTH consumer key: ")
        secret_key=input("Enter OAUTH consumer secret key: ")
        access_token_key=input("Enter OAUTH consumer Access Token key: ")
        access_secret_key=input("Enter OAUTH consumer Access Token Secret key: ")
    

    def get_tweets(self):
        


    def write_to_file(self):
        return 0;
    

if__name__="__main__":
    consumer_key=input("Enter OAUTH consumer key: ")
    secret_key=input("Enter OAUTH consumer secret key: ")
    access_token_key=input("Enter OAUTH consumer Access Token key: ")
    access_secret_key=input("Enter OAUTH consumer Access Token Secret key: ")
    extracter=Tweet_extractor()
    tweets=extractor.get_tweets()
    extractor.write_to_file()
